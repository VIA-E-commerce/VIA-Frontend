import styled from '@emotion/styled';

export const ContentsSection = styled.section`
  margin-top: 14.4rem;

  display: flex;
  flex-direction: column;
  gap: 4rem;

  nav {
    z-index: 100;
  }
`;
