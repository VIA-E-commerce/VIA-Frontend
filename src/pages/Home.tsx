import React from 'react';
import { GridSection, Layout } from '@/components';

const Home = () => {
  return (
    <Layout>
      <GridSection>홈</GridSection>
    </Layout>
  );
};

export default Home;
